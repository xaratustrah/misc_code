import java.awt.*;import java.awt.event.*;import java.text.*;import java.util.*;import java.io.*;import javax.swing.*;import javax.swing.table.*;import javax.swing.event.*; /**   * A main window with a JTable   *   * @author EAL April 2001   * @version 1.0.0   */public class MainWindow extends JFrame {    /** a reference to the table */   private JTable tableView;       /** main */	public static void main( String s[] ) {	   MainWindow mw = new MainWindow();	   mw.show();   }    /** Class constructor */      public MainWindow() {       // create the controls      createControls();       // put them in the window      buildWindow();       // size and place it      setSize(800,400);      setLocation(10,10);      setTitle("Test Window");       // listen for close events      addWindowListener(new w());   }       /**      * Helper function, creates all the widgets to be used on this window      */   private final void createControls() {      String rowData[][] = new String[][] {         { "row 1, col 1", "row 1, col 2", "row 1, col 3" },         { "row 2, col 1", "row 2, col 2", "row 2, col 3" } };      String colHeadings[] = new String[] {         "Column 1", "Column 2", "Column 3" };       // create a very simple table with some string array data      tableView = new JTable(rowData,colHeadings);   }       /**      * Layout the controls in the window      */   private final void buildWindow() {       // use a border layout   	getContentPane().setLayout( new BorderLayout(3,3));   	 // add the table to the center   	getContentPane().add(new JScrollPane(tableView), BorderLayout.CENTER);   }    /**      * Extend the window adapter so we don't have to override all window      * listeners, just windowClosing      */   class w extends WindowAdapter {      public void windowClosing( WindowEvent we ) {         System.exit(1);      }   }}